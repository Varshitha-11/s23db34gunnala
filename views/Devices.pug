extends layout

block content
  h1= title
  - var result = results 
  div(class="container-fluid")
  div(class="row")
   each r,index  in result 
    div(class="col-sm-2" onclick =`buy(${index})`) #{r.name}, #{r.ram}, #{r.color}
    script.
        function buy(a)
        {
            alert(`You purchased Item ${a+1}`)
        }